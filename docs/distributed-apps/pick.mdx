---
sidebar_position: 6
---
import Pick from "@site/src/components/distributed-apps/pick"

# Mở tệp trong trình duyệt để chọn tệp, thư mục hoặc dữ liệu
```javascript
web3.bzz.pick.file()
web3.bzz.pick.directory()
web3.bzz.pick.data()
```
Mở trình chọn tệp trong trình duyệt để chọn (các) tệp, thư mục hoặc dữ liệu.
### Thông số
không có
### Trả lại
`Promise` returning `Object`:Trả về tệp hoặc nhiều tệp.
### Ví dụ
```javascript
web3.bzz.pick.file()
.then(console.log);
> {
    ...
}
```
### Sự thử nghiệm
<Pick/>